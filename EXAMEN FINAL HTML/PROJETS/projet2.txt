#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) 
{
 float tab[10];
 int i;
 int compt=0;
 for(i=0;i<10;i++)
 {
 	printf("Entrer la valeur %d",i);
 	scanf("%d",&tab[i]);
 	if(tab[i]==0)
 	{
 	 compt=compt+1;
	 }
 }
 printf("le nombre de valeur nulle est %d",compt);
	return 0;
}