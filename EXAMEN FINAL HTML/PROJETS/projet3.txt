#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct Etudiant Etudiant;
struct Etudiant
{
 char nom[30];
 int mat;
 float moy;
};
int main(int argc, char *argv[]) 
{
  Etudiant E[5];
  int i;
  float max=0;
    for(i=0;i<5;i++)
    {
     printf("Entrer le nom de l'etudiant N %d",i);
     scanf("%s",E[i].nom);
     printf("Entrer le matricule de l'etudiant N %d",i);
     scanf("%d",&E[i].mat);
     printf("Entrer la moyenne de l'etudiant N %d",i);
     scanf("%f",&E[i].moy);
     if(E[i].moy>max)
     {
     	max=E[i].moy;
	 }
	}
	printf("les informations des 5 etudiants\n");
	for(i=0;i<5;i++)
	{
	printf("nom %s matricule %d moyenne %f\n",E[i].nom,E[i].mat,E[i].moy);
	}
	for(i=0;i<5;i++)
	{
		if(max==E[i].moy)
		{
		printf("\netudiant ayant la meilleure moyenne\n");
		printf("nom %s matricule %d moyenne %f",E[i].nom,E[i].mat,E[i].moy);
		}
	}
	return 0;
}